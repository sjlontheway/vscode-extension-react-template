{
    "extends": "../../tsconfig.base.json", // 首先导入 ⑤
    "compilerOptions": {
        "composite": true,
        "outDir": "../../dist/client" // 指定输入目录
    },
    "include": [
        "*.ts",
        "*/**"
    ]
}